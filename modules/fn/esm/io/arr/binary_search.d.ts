/**
 * Binary search O(log n)
 * @returns {number} index to splice the value into
 */
export default function binary_search<T extends string | number>(array: T[], value: T, low: number, high: number): number;
